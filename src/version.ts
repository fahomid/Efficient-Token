/** The server version — surfaced in the MCP initialize result, the health tool,
 *  and the heartbeat status file. Single source of truth, bumped by the release flow. */
export const VERSION = "1.0.5";
